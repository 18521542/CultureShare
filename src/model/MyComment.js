//NOTE: Comment is already a Component in library
//so I use MyComment as a custom class 
export default class MyComment{
    constructor(Id,Author, Time, Detail){
        this.Id=Id;
        this.Author=Author;
        this.Time=Time;
        this.Detail=Detail;
    }
}